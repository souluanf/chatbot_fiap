spring:
  application:
    name: chatbot

telegram:
  token: ${TELEGRAM-TOKEN}

open-weather-map:
  token: ${OPEN-WEATHER-TOKEN}

google:
  name: fiap-bot
  projectid: ${GOOGLE_CLOUD_PROJECT}
  languageCode: en-US

logging:
  level:
    web: fatal